return {
  "ChmaraX/herdr-nvim",
  opts = {},
  -- herdr side needs: herdr plugin install ChmaraX/herdr-nvim
  -- and in config.toml:
  -- [[keys.command]] key="prefix+e" type="plugin_action" command="chmarax.herdr-nvim.toggle"
  -- [[keys.command]] key="prefix+o" type="plugin_action" command="chmarax.herdr-nvim.pick-file"
}
